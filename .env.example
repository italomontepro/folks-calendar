PORT=3001
# Obrigatório em produção. Use uma senha longa e exclusiva.
APP_PASSWORD=
# Gere com: openssl rand -hex 32
SESSION_SECRET=
DATA_DIR=./data
# Origens HTTPS autorizadas a incorporar a agenda, separadas por espaço.
# Consulte a origem exata do seu CRM HELENA.
FRAME_ANCESTORS='self'
